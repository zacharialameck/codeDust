function setup() {
  createCanvas(400, 400);
}

function draw() {
  // when changing the numbers on background outomatically colors will change.
  background(220,400,200,400);
  //ellipse stand for the physical shape and those numbers reduced the shape of shape.
  ellipse(200,200,100,100);
  //the fill stand for changing the color inside the shape.
  fill(100)
  
  
  
}